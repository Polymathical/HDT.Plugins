<UserControl x:Class="HDT.Plugins.Custom.Controls.MulliganOddsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HDT.Plugins.Custom.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
        <ItemsControl ItemsSource="{Binding}" Height="Auto" Margin="0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock FontSize="14" FontFamily="Arial Black" Width="Auto" Margin="0,3,0,0">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} - {1} - {2}">
                                <Binding Path="LowerOdds"  />
                                <Binding Path="EqualOdds"  />
                                <Binding Path="HigherOdds" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
</UserControl>
